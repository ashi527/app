<?xml version="1.0" encoding="utf-8"?>
<!--
  DEV ONLY: allow cleartext (http) traffic to the local development backend so
  the emulator can reach it. 10.0.2.2 is the host machine as seen from the
  Android emulator; localhost works together with `adb reverse tcp:3000 tcp:3000`.
  Production traffic (your VPS over https) is unaffected and stays secure.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">172.239.103.172</domain>
    </domain-config>
</network-security-config>
